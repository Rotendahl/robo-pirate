System.put_env(
  "SLACK_TOKEN",
  "xxxx"
)

System.put_env(
  "BOT_TOKEN",
  "xxxx"
)

System.put_env(
  "PORT",
  "4000"
)

System.put_env(
  "BOARD",
    "bob, alice, etc" # Slack user names
)

System.put_env(
  "SLACK_URL",
  "https://slack.com/api/" # Set to localhost in dev
)

System.put_env(
  "announcemnts_id",
  "xxxxx"
  #cp "C0CNF7F0C"
)
