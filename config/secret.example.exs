System.put_env(
  "SLACK_TOKEN",
  "xxxx"
)

System.put_env(
  "BOT_TOKEN",
  "xxxx"
)

System.put_env(
  "PORT",
  "4000"
)

System.put_env(
  "BOARD",
  # Slack user names
  "bob, alice, etc"
)

System.put_env(
  "SLACK_URL",
  # Set to localhost in dev
  "https://slack.com/api/"
)

System.put_env(
  "announcemnts_id",
  "xxxxx"
)
