System.put_env(
  "SLACK_TOKEN",
  "Some-slack-token"
)

System.put_env(
  "BOT_TOKEN",
  "some-bot-token"
)

System.put_env(
  # The maximum age of a request in seconds before we rejct it
  "MAX_AGE",
  # 10 years, Slack will be gone by then
  "315360000"
)

System.put_env(
  "OAUTH_TOKEN",
  "some-oauth-token"
)

System.put_env(
  "SIGN_SECRET",
  "some-signing-secret"
)

System.put_env(
  "PORT",
  "4040"
)

System.put_env(
  "SLACK_PORT",
  "4050"
)

System.put_env(
  "LEGACY_TOKEN",
  "some-legacy-token"
)

System.put_env(
  "VOLUNTEER_CHANNELS",
  "volChannel1, volChannel2"
)

System.put_env(
  "CHILD_CHANNELS",
  "childChannel1, childChannel2"
)

System.put_env(
  "BOARD",
  # Slack user names
  "bob, alice, etc"
)

System.put_env(
  "PORT",
  "4040"
)

System.put_env(
  "SLACK_URL",
  # Set to localhost in dev
  "http://localhost"
)

System.put_env(
  "ANNOUNCEMNTS_ID",
  "announcemnt_id"
)

System.put_env(
  "INVITE_THREAD",
  "invite_thread_id"
)
